<!DOCTYPE html>
<html>
<head>
    <title>last attempt</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <!-- Font Awesome -->
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css">

  <link rel="stylesheet" href="main.css">
</head>



<body>
  <div class="container">

      <div class="navbarmenu">
        <div class="col-sm-12" "panel-primary" style="background-color: #000000 ; color: #93CCEA;">
          <h2 class="text-left"><strong>simpleBB</strong></h2>
          <h5>a modularized bulletin board</h5>
            <div class="navbar"
              <ul class="nav navbar-nav navbar-right navbar-right-custom">

                <li><a href="#">home</a></li>

                <li><a href="#">register</a></li>
                <li><a href="#">login</a></li>
                <div class="userStats">
                  <div class="username"><p>{{username if logged in}}</p>

                  </div>
                  <div class="logoutIfLoggedIn">
                    <button type="logout-button" name="logout">{{Logout}}</button>
                    <button type="reply-button" name="reply">{{if logged in, pass in reply button}}</button>

                    <!--note to self: the reply should post under the message body in a new div  -->


                  </div>

                </div>

              </ul>
            </div>

        </div>
      </div>
    <br>
  <table class="table table-striped">
     <thead>
       <div class="row">

            <tr class="table-header">
              <th class= "row col-md-4"><p>{{username}}</p></th>
              <th class= "row col-md-4">{{timestamp}}</th>

           </tr>
      </div>
    </thead>

    <tbody id="subtopics">

        <tr class="table-rows">
          <td><span id="messagebody"><p>{{message body}}</p></span>

        </tr>
    </tbody>
  </table>


  <!-- Footer Region -->
  <div class="row">
    <div class="col-md-12">

      <!-- Line Break followed by closing -->
      <hr>
      <h5 class="text-center"><medium>&copy simpleBB. All posts copyright their original authors</medium></h5>

    </div>
  </div>




  <!-- jQuery JS -->
  <script src="https://code.jquery.com/jquery.js"></script>

  <!-- Code to the JavaScript File -->
  </body>
  </html>
